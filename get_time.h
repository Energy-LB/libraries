#ifndef __FILES_Ht
#define __FILES_Ht

void tempo1();

void tempo2();

int gravaArquivo(char * nomeArquivo, char *msglog, char *tempo);

void tempoFinal(const char *medida, char *nomeArquivo, char *msglog);

float tempo3(const char *medida);

#endif
